// contexts/mediasoup/handlers/transportHandlers.ts
import { Device } from 'mediasoup-client';
import { MediasoupState } from '../types';

export const createTransports = async (state: MediasoupState) => {
  // Реализация создания транспортов
};

export const handleTransportCreated = async (
  data: any,
  state: MediasoupState
) => {
  // Реализация обработки создания транспорта
};